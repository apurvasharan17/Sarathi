import { ReactNode } from 'react';
interface LayoutProps {
    children: ReactNode;
    title?: string;
    showBack?: boolean;
    backgroundImageUrl?: string;
    backgroundOverlayOpacity?: number;
}
export declare function Layout({ children, title, showBack, backgroundImageUrl, backgroundOverlayOpacity, }: LayoutProps): any;
export {};
//# sourceMappingURL=Layout.d.ts.map